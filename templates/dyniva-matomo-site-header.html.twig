{{ attach_library('dyniva_matomo/wrapper') }}
<div class="dyniva_matomo_wrapper">
  <div class="card-1">
    <div class="card-header">
      <h3>{{ content.label }} {{ content.today }}</h3>
      <div class="ext">{{ content.admin }}</div>
    </div>
  </div>
  <div class="spacer"></div>
</div>
